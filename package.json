{
  "name": "water-tank-cloudflare-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy .",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
